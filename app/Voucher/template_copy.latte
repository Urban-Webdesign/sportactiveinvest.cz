<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>{$name} - {$variableSymbol}</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">

    <style>

        html {
            margin: 0;
        }

        body {
            width: 1704px;
            height: 1200px;
            overflow: hidden;
            margin: 0;
            padding: 0;
            display: block;
            position: relative;
            background: url('https://luciesvecena.cz/dist/img/voucher/voucher3.jpeg') no-repeat;
            background-size: contain;
            font-family: 'Roboto', sans-serif;
            font-weight: 700;
        }

        .name, .lessons, .variableSymbol {
            position: absolute;
            font-size: 24px;
            color: #000000;
            width: 100%;
        }

        .validity {
            position: absolute;
            font-size: 18px;
            color: #ffffff;
            width: 100%;
        }

        .name { top: 200px; left: 340px}
        .lessons { top: 303px; left: 340px}
        .variableSymbol { top: 303px; left: 540px;}
        .validity { top: 365px; left: 515px}
    </style>
</head>
<body>
    <div class="name">{$name}</div>
    <div class="lessons">{$lessons}</div>
    <div class="variableSymbol">{$variableSymbol}</div>
    <div class="validity">{$validity}</div>
</body>
</html>